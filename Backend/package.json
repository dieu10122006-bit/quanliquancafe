{
  "name": "cafe-management-system",        // Tên project
  "version": "1.0.0",                      // Version
  "main": "app.js",                        // File chính
  "scripts": {
    "start": "node app.js",               // Chạy server
    "dev": "nodemon app.js"               // Chạy dev auto reload
  },
  "dependencies": {
    "express": "^4.18.2",                 // Framework backend
    "mysql2": "^3.6.0",                   // Kết nối MySQL
    "dotenv": "^16.0.3",                  // Biến môi trường
    "cors": "^2.8.5",                     // Cho phép gọi API
    "bcrypt": "^5.1.0",                   // Hash password
    "jsonwebtoken": "^9.0.0",             // JWT auth
    "nodemailer": "^6.10.1"               // Gửi email
  }
}